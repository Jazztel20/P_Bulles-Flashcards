<header>
  <div class="container-header">
    <div class="titre-header">
      <h1>Decks</h1>
    </div>

    <div class="login-container">
      @if(auth.user)
        <span>
          Connecté : <strong>{{ auth.user.username }}</strong>
        </span>

        <form action="{{ route('auth.handleLogout') }}" method="post" style="display:inline;">
          {{ csrfField() }}
          <button type="submit" class="btn btn-login">
            Se déconnecter
          </button>
        </form>
      @else
        <form action="{{ route('auth.handleLogin') }}" method="post" style="display:inline;">
          {{ csrfField() }}

          <label for="username"></label>
          <input type="text" name="username" id="username" placeholder="Login" required />

          <label for="password"></label>
          <input type="password" name="password" id="password" placeholder="Mot de passe" required />

          <button type="submit" class="btn btn-login">
            Se connecter
          </button>
        </form>

        <a href="{{ route('auth.signup') }}" class="btn btn-login" style="text-decoration:none;">
          S’enregistrer
        </a>
      @endif
    </div>
  </div>

  <nav>
    <h2>Zone pour le menu</h2>
    <a href="{{ route('home') }}">Accueil</a>

    @if(auth.user)
      <a href="{{ route('decks.create') }}">Ajouter un deck</a>
    @endif
  </nav>
</header>
