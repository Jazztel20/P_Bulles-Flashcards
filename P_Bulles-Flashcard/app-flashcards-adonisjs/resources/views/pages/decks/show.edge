@component('components/layout', { title: deck.title })
<h3>{{ deck.title }}</h3>

<p>{{ deck.description || 'Aucune description.' }}</p>

<div class="user-footer">
  <a class="action-link" href="{{ route('decks.edit', { id: deck.id }) }}">Modifier</a>
  <a class="action-link" href="{{ route('home') }}">Retour</a>
</div>
@endcomponent
